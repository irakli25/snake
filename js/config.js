const gameStartSpeed = 500; // millisecond 
const speedChange = 2; // times in level
const levelUp = 5; // after every this point level ++
const elementSize = 10; // object size in px
const screenSize = 28; // game screen

export {gameStartSpeed, speedChange, levelUp, elementSize, screenSize};